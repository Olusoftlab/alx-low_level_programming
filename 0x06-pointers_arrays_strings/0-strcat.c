#include "main.h"
/**
 * _strcat - add two strings
 * @ddest: cp
 * @src: kdkd
 * Return: two concat str
 */

char *_strcat(char *dest, char *src)
{
	int i, n;

	for (i = 0; dest[i] != '\0' i++)
	{
	}

	for (n = 0; (dest[i + n] = *src++) != '\0'; n++)
	{
	}

	return (dest);
}
