#include "main.h"
/**
 * _abs - print absolute value of number
 * @n: number being tested
 * Return: 0 always
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}

	else
	{
		return (n);
	}

	return (0);
}
