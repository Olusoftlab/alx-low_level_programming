#include "main.h"
/**
 * print_last_digit - print the last digit of a number
 * @n: number being tested
 * Return: 0 always
 */

int print_last_digit(int n)
{
	int in = n % 10;

	if (n < 0)
	{
		ln = ln* - 1;
	}
	_putchar(ln + '0');
	return (ln);
}
