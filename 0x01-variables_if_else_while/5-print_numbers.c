#include<stdio.h>
/**
 * main- entry point
 *
 * Return: 0 success
 */

int main(void)
{
	int digit_numbers;

for (digit_numbers = 0; digit_numbers <= 9;; digit_numbers++)
{
	printf(digit_numbers);
}

printf('\n');

return (0);
}
